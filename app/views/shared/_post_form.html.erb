<%= form_for(@post) do |f| %>
	<%= render 'shared/error_messages', object: f.object %>
	<div class="field">
		<%= f.text_area :content, :class => "tinymce", :rows => 40, :cols => 120, placeholder: "Write a post" %>
	</div>
	<%= f.submit "Post", class: "btn btn-primary" %>
<% end %>